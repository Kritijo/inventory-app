<h3>Genres ⭐️</h3>
<form class="add-form" action="/newGenre" method="POST">
  <input name="name" required />
  <button>Add Genre</button>
</form>
<ul>
  <% genres.forEach(genre => { %>
  <li>
    • 
    <a href="genre/<%=genre.id%>%>"><%= genre.name %></a>
  </li>
  <% }) %>
</ul>
